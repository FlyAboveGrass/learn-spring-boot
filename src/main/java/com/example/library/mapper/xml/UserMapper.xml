<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须对应Mapper接口的全限定名 -->
<mapper namespace="com.example.library.mapper.UserMapper">
    <update id="update" parameterType="com.example.library.dto.request.UserUpdateDTO">
        UPDATE user
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <!-- 如果 role 为 user 或者 admin 则更新 -->
            <if test="role == 'user' or role == 'admin'">
                role = #{role},
            </if>
        </set>
        WHERE user_id = #{userId}
    </update>
</mapper>